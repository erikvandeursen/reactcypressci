version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/install:
        build: 'npx react-scripts run'
      - cypress/run:
        requires:
          - cypress/install
        wait-on: "http://localhost:3000"
